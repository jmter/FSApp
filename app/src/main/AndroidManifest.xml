<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jmt.fsapp">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission
        android:name="android.permission.CAMERA"
        android:screenOrientation="landscape" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ReciboDeSueldoViewer"></activity>
        <activity android:name=".Activities.MisRecibosDeSueldo" />
        <activity android:name=".Activities.ParteDeMaquina" />
        <activity android:name=".Activities.PerfilEquipo" />
        <activity android:name=".Activities.AgregarEquipo" />
        <activity android:name=".Activities.CameraScanDoc" />
        <activity android:name=".Activities.InformacionPersonal" />
        <activity android:name=".Activities.Equipos" />
        <activity
            android:name=".Activities.Login"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:noHistory="true" />
    </application>

</manifest>